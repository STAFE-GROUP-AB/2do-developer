{
  "name": "weekly-project-cleanup",
  "description": "Weekly project maintenance and cleanup every Friday at 5 PM",
  "schedule": "0 17 * * 5",
  "enabled": true,
  "tasks": [
    {
      "type": "custom_command",
      "config": {
        "command": "git status && git log --oneline -10",
        "working_dir": "."
      }
    },
    {
      "type": "ai_prompt",
      "config": {
        "prompt": "Analyze the git status and recent commits. Suggest cleanup tasks, documentation updates, and next steps for the project.",
        "model": "auto"
      }
    },
    {
      "type": "add_todo",
      "config": {
        "content": "Weekly project review: Update documentation, clean up unused files, and plan next sprint",
        "type": "general",
        "priority": "medium"
      }
    }
  ]
}